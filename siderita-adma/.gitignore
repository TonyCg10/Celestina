# Cargo build output
/target/

# cxx-qt / Qt tooling, regenerated per build (Siderita is Cargo + cxx-qt, not
# CMake). A CMake build, if ever added, is covered by the root .gitignore.
.qmlls.ini
*.qmlc
compile_commands.json
target/stage/
