# AND-5 — The phone's files over the link

- **Opened:** 2026-09-10
- **Closed:** 2026-09-13
- **Plan ID:** storage
- **Status:** done
- **Authorization:** the author said "sigue con MAG-P7" on 2026-09-10 with
  `AND-4` implemented
- **Scope:** magnetita-android
- **Implementation checkpoint:** AND-5
- **Author-validation checkpoint:** `VAL-MAG-15` in Magnetita's
  [`../../../../magnetita/VALIDATION.md`](../../../../magnetita/VALIDATION.md),
  passed on 2026-09-13
- **Successor:** AND-6

## Hypothesis

The folder the person picks once in the system's tree picker is the root
the desktop browses: the storage access framework's persisted grant, the
documents contract for listings, entries, ranges and changes, and nothing
the platform does not give. The wire's paths map onto document ids by
appending, so no path is ever built from strings the desktop chose.

## Tangible outcome

The device screen's "Archivos" row; Siderita browsing that folder through
the daemon's mount.

## Scope

- `AND-5-A` — the storage adapter over the document tree (list, stat,
  read, write, mkdir, rename, delete), the grant row, the state sent on
  connect and on grant, the requests as signals; the path arithmetic pure
  and tested.
- `AND-5-B` — the whole phone as the root through the all-files grant,
  since the tree picker refuses the root; the same requests over plain
  files; the tree stays as the narrower choice.

- `AND-5-C` — one walk per directory: the listing kept ten seconds so
  the desktop's pages of it cost no second walk.

- `AND-5-D` — the link back on its own after a boot and after an update
  of the application, so the mount and everything else return with no
  hand on the phone.

## Exclusions

- The `MediaStore` beyond the shared root.

## Build order

1. `AND-5-A`, `-B`, `-C`, `-D`.

## Implementation exit

The path arithmetic and the signals have JVM tests, `lintRelease` passes,
the release build is on the S25U; the browse itself is `VAL-MAG-15`.

## Change and commit ledger

| Unit | Commit prefix | Status | Files / areas | Diffstat | Intended change | Automated evidence | Author validation |
|---|---|---|---|---|---|---|---|
| AND-5-D | `magnetita-android:` | done | [inventory](../../inventories/2026-09-10-storage/AND-5-D.numstat.tsv) | 6 files, +92/-1 | The link restarts after a boot and after an update | [record](../../evidence/2026-09-10-link-restart.md) | `VAL-MAG-14` |
| AND-5-C | `magnetita-android:` | done | [inventory](../../inventories/2026-09-10-storage/AND-5-C.numstat.tsv) | 5 files, +77/-3 | One walk per directory across the desktop's pages | [record](../../evidence/2026-09-10-listing-pages.md) | `VAL-MAG-15` |
| AND-5-B | `magnetita-android:` | done | [inventory](../../inventories/2026-09-10-storage/AND-5-B.numstat.tsv) | 11 files, +192/-13 | The whole phone as the root with the all-files grant | [record](../../evidence/2026-09-10-storage-whole-phone.md) | `VAL-MAG-15` |
| AND-5-A | `magnetita-android:` | done | [inventory](../../inventories/2026-09-10-storage/AND-5-A.numstat.tsv) | 23 files, +633/-71 | The storage adapter over the shared document tree; `AND-4` archived and `AND-5` opened | [record](../../evidence/2026-09-10-storage-tree.md) | `VAL-MAG-15` |
