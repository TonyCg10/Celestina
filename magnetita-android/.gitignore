# Build outputs, IDE state and the machine's SDK location stay out.
/.gradle/
/build/
/app/build/
/.idea/
/local.properties
/.kotlin/
*.iml
/captures/
# The author's signing key and the properties that name it.
/keystore.properties
*.jks
*.keystore
