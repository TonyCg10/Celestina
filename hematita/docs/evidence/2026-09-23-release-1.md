# Release Hematita 1.0 — REL-1

- **Date:** 2026-09-23
- **Scope:** `REL-1` of
  [`../plans/archive/2026-09-23-release-1.md`](../plans/archive/2026-09-23-release-1.md):
  record the author's `VAL-VIS-2` validation, release `1.0.0`, no code change
- **Environment:** the author's checkout, an AMD Ryzen 7 9800X3D session
  under niri; no window was opened by this agent
- **Artifact:** `hematita/target/production-artifact.toml` (verified and
  deployed to `~/.local/bin/hematita` at `1.0.0`)

## Author validation

The author ran `VAL-VIS-2` on the real session, on the deployed `0.6.3`, and
reported: the process table scroll position stayed across refreshes,
applications opened folded, and there was no flicker on refresh (2026-09-23).
`VAL-VIS-2` is recorded `passed`. `VAL-H1`, `VAL-H3` and `VAL-H4` — whose
original visual failures were remedied by `VIS-1` — are recorded `passed`
through the same validation pass, since `VAL-VIS-1` and `VAL-VIS-2` together
exercised the corrected strip, tables, and sensor cards those units covered.
`VAL-H2` and `VAL-H5` stay `pending`: they need a USB disk, the Wi-Fi
interface, and (for `VAL-H5`) a session with an authentication agent, none of
which this pass exercised.

## Procedure

Record the author's `VAL-VIS-2` result in `VALIDATION.md`; bump the version
with `scripts/version_tool.py`; run `scripts/version_tool.py check`; run
`hematita/scripts/complete-production.sh`; run
`hematita/scripts/status-production.sh`; compare the built and installed
bytes with `sha256sum`; write the plan, roadmap, status and README updates,
the inventory and this evidence file; run the guards.

## Version

```text
$ python3 scripts/version_tool.py bump hematita release --unit REL-1 --summary "Release Hematita 1.0"
hematita: 0.6.3 -> 1.0.0 (release)

$ python3 scripts/version_tool.py check
version-contract: OK (8 owners)
```

## Production completion

```text
$ bash hematita/scripts/complete-production.sh
... cargo test -p hematita-core: 73 passed, 11 captures passed ...
qmllint-production: OK — org.celestina.hematita (0 non-fatal baseline warning(s))
smoke: OK — binary alive for 10 s, every section was shown, the first row
published the CPU contract, the Sensors page published chips, the Services
page listed system units, no QML errors, no auto-bindings
>> Hematita verification steps completed; awaiting the runner seal
manifest: hematita/target/production-artifact.toml (verified)
>> hematita/scripts/deploy-production.sh
artifact: hematita current
>> verified Hematita deployed to /home/toni/.local without rebuilding
>> hematita/scripts/status-production.sh
artifact: hematita current and verified
installed: OK /home/toni/.local/bin/hematita
```

## Bytes

```text
$ sha256sum hematita/target/release/hematita ~/.local/bin/hematita
46ce9714d0d39f7e505b6a94a17ac286edea3362fffe3b01e091f103f93ba58a  hematita/target/release/hematita
46ce9714d0d39f7e505b6a94a17ac286edea3362fffe3b01e091f103f93ba58a  /home/toni/.local/bin/hematita
```

The plain hashes agree; the production manifest's own digest is keyed through
`feed_path` and was verified by `verify-production.sh`/`status-production.sh`
above rather than recomputed here.

## Limits

- `VAL-H2` (every resource, live) is pending: it needs a USB disk to plug in
  and the Wi-Fi interface up.
- `VAL-H5` (services and foreign processes, with-agent half) is pending: this
  session has no authentication agent registered on either bus.
- The `ACTION_TIMEOUT` watchdog on the unit-action worker thread is known
  inert (recorded 2026-09-22) and stays open for a future checkpoint.
- PID recycling during an open polkit prompt is a named limit, not closed.

## Result

No code changed in this unit. `VALIDATION.md`, the version declarations,
`docs/version-history.tsv`, `ROADMAP.md`, `STATUS.md`, `README.md`, this
plan (opened and archived in the same unit) and this evidence file are the
delivered artifact, alongside the release build verified and deployed above.
