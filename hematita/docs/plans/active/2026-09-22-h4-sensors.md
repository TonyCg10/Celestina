# H4 — Sensors

- **Opened:** 2026-09-22
- **Plan ID:** h4-sensors
- **Status:** active
- **Authorization:** the author asked to open the H4 plan on 2026-09-22
- **Scope:** hematita
- **Implementation checkpoint:** H4
- **Author-validation checkpoint:** `VAL-H4` in [VALIDATION.md](../../../VALIDATION.md)

## Hypothesis

Every channel hwmon exposes can be read as a typed value with its unit, its
kernel limits and the session's extremes, from files alone, and shown per
chip without the monitor's idle cost growing.

## Tangible outcome

The installed 0.5.0 has a Sensors page listing every chip and channel of the
author's machine, and the process table keeps its success message, its
column alignment and a debounced search.

## Scope

- `H4-A` — `hematita-core`: `sensors`, captures, the deferred cgroup
  regression test.
- `H4-B` — sampler sensors section, `HematitaSensors`, the page.
- `H4-C` — the review's corrections to `H4-B`.
- `H4-Z` — implementation exit and 0.5.0.

## Exclusions

- Alerts, fan control, history graphs per channel (a later phase if wanted),
  non-hwmon sources.

## Build order

1. `H4-A`, then `H4-B`, `H4-C`, `H4-Z`.

## Implementation exit

`scripts/complete-production.sh` succeeds; the installed binary lists every
hwmon chip and channel with its unit, its kernel limits and the session's
extremes.

## Change and commit ledger

| Unit | Commit prefix | Status | Files / areas | Diffstat | Intended change | Automated evidence | Author validation |
|---|---|---|---|---|---|---|---|
| H4-A | `hematita:` | done | [inventory](../../inventories/2026-09-22-h4-sensors/H4-A.numstat.tsv) | 14 files, +802/-6 | `hwmon` discovery: `ChannelKind`, `Channel`, `Chip`, `parse_channel_file`, `convert`, `discover`, captures of the processor, GPU and board chips, the deferred cgroup regression test, H4 opened | [core](../../evidence/2026-09-22-h4-core.md) | `VAL-H4` |
| H4-B | `hematita:` | done | [inventory](../../inventories/2026-09-22-h4-sensors/H4-B.numstat.tsv) | 16 files, +1033/-41 | Sampler sensors section read every tick, `publish::thermal_load`, `HematitaSensors`, the Sensors page with its chip card and channel row, and the six process-table items parked by `H3-E` | [sensors page](../../evidence/2026-09-22-h4-sensors-page.md) | `VAL-H4` |
| H4-C | `hematita:` | done | [inventory](../../inventories/2026-09-22-h4-sensors/H4-C.numstat.tsv) | 13 files, +309/-38 | The review's four important findings: the `hwmon` facts are re-validated by the chip's name each tick (a re-bound index cannot inherit another device's `crit`, and the extremes key gained the name), the sensor rows are stable across ticks (an integer model, so focus survives), the smoke steps through every section so no page goes unconstructed, and `channelStates` is removed from the published contract: unreachable, since `discover` skips an unreadable channel. Plus the unmapped chip ordinal and the search's Return | [sensor gates](../../evidence/2026-09-22-h4-sensor-gates.md) | `VAL-H4` |
| H4-Z | `hematita:` | planned | `hematita/`, `docs/version-history.tsv` | — | Implementation exit, 0.5.0, documents closed, plan archived | `scripts/complete-production.sh` | `VAL-H4` |
