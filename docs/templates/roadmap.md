# Project implementation roadmap

- **Status:** planned
- **Active implementation checkpoint:** none
- **Related author validation:** `VAL-ID` in `VALIDATION.md` (does not block)

Use `active` or `blocked` only after replacing `none` with the exact checkpoint
id of one active plan owned by this roadmap. `planned`, `idle` and `done` keep
`none`; `idle` means there is no queued implementation as well as no open
checkpoint.

## Hypothesis and tangible outcome

State one falsifiable implementation claim and what can be built or inspected.

## Scope

- Included implementation.

## Exclusions

- Explicitly later or out of scope.

## Build order

| Unit | Status | Dependency | Implementation result | Agent evidence |
|---|---|---|---|---|
| ID-A | planned | none | result | command or evidence target |

## Implementation exit

Define the integrated agent-executable proof. Close the checkpoint when code and
this evidence pass; do not add pending manual tests here.

## Closed evidence

Link dated evidence or exact ledger rows. Keep historical results concise.
