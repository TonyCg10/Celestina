# The own-protocol program closed on the author's use — VAL-MAG-11, -12, -14, -15

- **Date:** 2026-09-13
- **Scope:** `MAG-D1-A` of
  [`../plans/active/2026-09-13-app-design.md`](../plans/active/2026-09-13-app-design.md):
  the checkpoint opened, `MAG-M1` archived, and the author-validation checkpoints of `MAG-P3` through `MAG-P7`
  in [`../../VALIDATION.md`](../../VALIDATION.md), recorded when the
  author asked to close the whole development and leave one checkpoint
  open, the applications' design
- **Environment:** the author's desktop and the S25U, in daily use since
  2026-09-09
- **Artifact:** the deployed `magnetitad`, `magnetita` and the release
  build on the S25U

## What the author observed, in the order the checkpoints were set

- `VAL-MAG-11`: the S25U paired by scanning the desktop's QR on 2026-09-09
  ("ya escanee") and has held its session since, reconnecting on its
  own after every reinstall.
- `VAL-MAG-12`: the daily set over the own wire since 2026-09-10, with
  the KDE Connect wire removed that day: clipboard, notifications (the
  media notification the author asked to switch off), files, media,
  contacts, SMS and calls.
- `VAL-MAG-14`: the mirror over the link, brought to the author's
  satisfaction through 2026-09-12 and 2026-09-13 with the recordings the
  author sent: the window opens from the shell's plugin with no `adb`,
  shows the phone at once, follows its turns, sizes itself to the tile on
  any output, controls the phone, and turns its screen off; "listo ya
  funciona".
- `VAL-MAG-15`: the phone browsed from Siderita through the FUSE mount,
  whole phone as the root, since 2026-09-10 ("funciona").
- `VAL-MAG-13` stays deferred: the phone's trackpad screen was not
  exercised; the mirror window is the control the author uses.

## Procedure

The author's daily use of the deployed pieces, and the author's word in
chat; no script.

## Result

The program's checkpoints `MAG-P0` through `MAG-P7` and `MAG-M1` are
closed; `MAG-D1` and `AND-6`, the design of the two applications, are the
one open checkpoint on each project.

## Limits

- These are the author's observations as reported, not a scripted run;
  the reboot `VAL-MAG-14` names was lived through the days of use, not
  staged.
