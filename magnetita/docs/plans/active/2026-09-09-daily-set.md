# MAG-P4 — The daily set on the own wire

- **Opened:** 2026-09-09
- **Plan ID:** daily-set
- **Status:** active
- **Authorization:** the author said "sigue" on 2026-09-09 with `MAG-P3`'s
  exit met
- **Scope:** magnetita, magnetitad, magnetita-mobile, magnetita-peer
- **Implementation checkpoint:** MAG-P4
- **Author-validation checkpoint:** `VAL-MAG-12` in
  [`../../../VALIDATION.md`](../../../VALIDATION.md)

## Hypothesis

The daemon's plugin modules keep their domain and gain the own wire one
capability at a time, each proven by `magnetita-peer` against the daemon in
a loopback test before the phone touches it; the phone side stays a thin
adapter over the core.

## Tangible outcome

The author's daily set — clipboard, notifications, file share, media,
contacts, SMS, telephony — working end to end on the own wire, with the
Rust half here and the Kotlin half under `magnetita-android`'s `AND-2`.

## Scope

- `MAG-P4-A` — clipboard both ways: the own-wire session drains the shared
  pending slot the desktop watcher fills, writes the phone's text through
  the Wayland adapter, asks for the phone's clipboard on connect; the core
  sends and decodes; the peer carries `--clipboard`.
- `MAG-P4-B` — notifications with actions and replies.
- `MAG-P4-C` — resumable file share both ways under revocation.
- `MAG-P4-D` — media control both ways.
- `MAG-P4-E` — contacts sync and name resolution.
- `MAG-P4-F` — SMS conversations, send, receive, MMS attachments.
- `MAG-P4-G` — call state, mute, answer, hang up; the shell's phone menu.

## Exclusions

- Every Kotlin file: `magnetita-android`'s `AND-2`.
- Commands, input and mirror: `MAG-P5` onwards.

## Build order

1. `MAG-P4-A` through `-D` in any order after `MAG-P3`; `-E` before `-F`
   and `-G`.

## Implementation exit

`magnetita-peer` exercises every capability against the daemon in tests,
the application's JVM tests cover each adapter, and
`scripts/complete-production.sh` passes with both wires.

## Change and commit ledger

| Unit | Commit prefix | Status | Files / areas | Diffstat | Intended change | Automated evidence | Author validation |
|---|---|---|---|---|---|---|---|
| MAG-P4-A | `magnetita:` | done | [inventory](../../inventories/2026-09-09-daily-set/MAG-P4-A.numstat.tsv) | 19 files, +481/-83 | Clipboard both ways on the own wire; `MAG-P3` archived and `MAG-P4` opened | [record](../../evidence/2026-09-09-clipboard-own-wire.md) | `VAL-MAG-12` |
| MAG-P4-B | `magnetita:` | planned | `magnetitad/src/link_wire/`, `magnetita-mobile/` | — | Notifications with actions and replies | peer tests | None |
| MAG-P4-C | `magnetita:` | planned | `magnetita-link/`, `magnetitad/src/link_wire/` | — | Resumable file share both ways | peer tests | None |
| MAG-P4-D | `magnetita:` | planned | `magnetitad/src/link_wire/`, `magnetitad/src/media.rs` | — | Media control both ways | peer tests | None |
| MAG-P4-E | `magnetita:` | planned | `magnetitad/src/link_wire/` | — | Contacts sync and name resolution | peer tests | None |
| MAG-P4-F | `magnetita:` | planned | `magnetitad/src/link_wire/`, `magnetita/qml/` | — | SMS conversations on the desktop | peer tests | None |
| MAG-P4-G | `magnetita:` | planned | `magnetitad/src/link_wire/`, `magnetitad/src/devices.rs` | — | Call state on `Devices1` and the shell menu | peer tests | None |
