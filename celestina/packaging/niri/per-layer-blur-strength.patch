diff --git a/niri-config/src/appearance.rs b/niri-config/src/appearance.rs
index a1c8327..b6dec21 100644
--- a/niri-config/src/appearance.rs
+++ b/niri-config/src/appearance.rs
@@ -1065,6 +1065,10 @@ pub struct BackgroundEffectRule {
     pub noise: Option<FloatOrInt<0, 1000>>,
     #[knuffel(child, unwrap(argument))]
     pub saturation: Option<FloatOrInt<0, 1000>>,
+    #[knuffel(child, unwrap(argument))]
+    pub passes: Option<u8>,
+    #[knuffel(child, unwrap(argument))]
+    pub offset: Option<FloatOrInt<0, 100>>,
 }
 
 /// Resolved background effect rule.
@@ -1087,11 +1091,18 @@ pub struct BackgroundEffect {
 
     pub noise: Option<f64>,
     pub saturation: Option<f64>,
+
+    /// Blur strength overrides for this rule.
+    ///
+    /// - `None`: use the global `blur {}` settings
+    /// - `Some(x)`: override for surfaces matching this rule
+    pub passes: Option<u8>,
+    pub offset: Option<f64>,
 }
 
 impl MergeWith<BackgroundEffectRule> for BackgroundEffect {
     fn merge_with(&mut self, part: &BackgroundEffectRule) {
-        merge_clone_opt!((self, part), xray, blur);
+        merge_clone_opt!((self, part), xray, blur, passes);
 
         if let Some(x) = part.noise {
             self.noise = Some(x.0);
@@ -1100,6 +1111,10 @@ impl MergeWith<BackgroundEffectRule> for BackgroundEffect {
         if let Some(x) = part.saturation {
             self.saturation = Some(x.0);
         }
+
+        if let Some(x) = part.offset {
+            self.offset = Some(x.0);
+        }
     }
 }
 
diff --git a/src/render_helpers/background_effect.rs b/src/render_helpers/background_effect.rs
index d148b6c..60e0f91 100644
--- a/src/render_helpers/background_effect.rs
+++ b/src/render_helpers/background_effect.rs
@@ -36,6 +36,11 @@ pub struct Options {
     pub xray: bool,
     pub noise: Option<f64>,
     pub saturation: Option<f64>,
+    /// Per-surface blur strength overrides; `None` falls back to the global
+    /// `blur {}` settings. Only meaningful for the non-xray path: the xray
+    /// backdrop is blurred once for the whole output.
+    pub passes: Option<u8>,
+    pub offset: Option<f64>,
 }
 
 impl Options {
@@ -126,6 +131,8 @@ impl BackgroundEffect {
             xray: effect.xray == Some(true),
             noise: effect.noise,
             saturation: effect.saturation,
+            passes: effect.passes,
+            offset: effect.offset,
         };
 
         // If we have some background effect but xray wasn't explicitly set, default it to true
@@ -170,7 +177,15 @@ impl BackgroundEffect {
         // Use noise/saturation from options, falling back to blur defaults if blurred, and
         // to no effect if not blurred.
         let blur = self.options.blur && !self.blur_config.off;
-        let blur_options = blur.then_some(BlurOptions::from(self.blur_config));
+        let mut blur_options = blur.then_some(BlurOptions::from(self.blur_config));
+        if let Some(opts) = &mut blur_options {
+            if let Some(passes) = self.options.passes {
+                opts.passes = passes;
+            }
+            if let Some(offset) = self.options.offset {
+                opts.offset = offset;
+            }
+        }
         let noise = if blur { self.blur_config.noise } else { 0. };
         let noise = self.options.noise.unwrap_or(noise) as f32;
         let saturation = if blur {
